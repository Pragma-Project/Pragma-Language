# Pragma
Low-level systems language with high-level syntax, transpiles to C, can sanitize and optimize code, as well as prove mathematically the same as Rust anyone's code as safety-verified with its SHA-256 hash on blockchain.
